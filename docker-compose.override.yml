services:
  db:
    ports:
      - "127.0.0.1:3306:3306"

  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - "127.0.0.1:8000:80"
    environment:
      PMA_HOST: db
      PMA_ARBITRARY: 1
    networks:
      - wikisnet
